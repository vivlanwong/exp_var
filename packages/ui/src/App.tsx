import { type Component } from 'solid-js';
  

const App: Component = () => {
  // var [count, setCount] = createSignal(0)
  // var i = 0
  // setInterval(function () {
  //   setCount(i++)
  // })

  return (
    <div>
      hello plugin
    </div>
  );
}; 

export default App;
